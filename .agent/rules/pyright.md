---
description: Type checking rules
---
# Pyright Rule

Always use `pyright` for Python static type checking.
- Ensure all new Python code includes appropriate type hints.
- Run `pyright` to catch type errors before executing the code.
