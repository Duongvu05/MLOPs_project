---
description: Code formatting and linting rules
---
# Ruff Rule

Always use `ruff` to lint and format Python code.
- Run `ruff check .` to check for linting errors.
- Run `ruff check --fix .` to auto-fix linting errors.
- Run `ruff format .` to format the codebase.
