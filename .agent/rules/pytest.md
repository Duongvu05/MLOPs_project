---
description: Unit testing rules
---
# Pytest Rule

Always use `pytest` for writing and running unit tests.
- Place tests in a `tests/` directory with filenames starting with `test_`.
- Run tests via `pytest` command.
- Ensure tests cover core functionalities and edge cases.
