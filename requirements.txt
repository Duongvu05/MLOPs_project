# MRI Phenotyping Project - Requirements
# For GPU training on NVIDIA RTX 3060 with CUDA support

# ============================================================================
# IMPORTANT: PyTorch with CUDA Support
# ============================================================================
# RECOMMENDED: Install PyTorch via pip to avoid MKL conflicts:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# OR for CUDA 12.1:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 
# Alternative (if conda is preferred, may require fixing MKL issues):
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
# 
# Then verify CUDA availability:
# python -c "import torch; print(torch.cuda.is_available()); print(torch.version.cuda)"
# ============================================================================

# Core Deep Learning & Medical Imaging
monai[all]>=1.3.0              # Medical imaging AI framework (includes all optional dependencies)
nibabel>=5.0.0                  # Neuroimaging file I/O (NIfTI support)
SimpleITK>=2.3.0                # Image processing and registration
scikit-image>=0.21.0            # Image processing utilities

# DICOM Processing
pydicom>=2.4.0                  # DICOM file reading and writing
highdicom>=0.22.0               # High-level DICOM utilities (optional, for advanced DICOM operations)

# Data Processing & Analysis
numpy>=1.24.0                   # Numerical computing
pandas>=2.0.0                   # Data manipulation and analysis
scipy>=1.11.0                   # Scientific computing

# Machine Learning & Statistics
scikit-learn>=1.3.0             # Machine learning utilities
scikit-image>=0.21.0            # Image processing (duplicate but common)

# Visualization
matplotlib>=3.7.0               # Plotting and visualization
seaborn>=0.12.0                 # Statistical data visualization
plotly>=5.17.0                  # Interactive visualizations (optional)

# Experiment Tracking & Logging
wandb>=0.15.0                   # Weights & Biases for experiment tracking
tensorboard>=2.13.0             # TensorBoard for visualization (optional, PyTorch uses it)

# Utilities
tqdm>=4.66.0                    # Progress bars
pillow>=10.0.0                  # Image processing
opencv-python>=4.8.0            # Computer vision library
imageio>=2.31.0                 # Image I/O utilities
pyyaml>=6.0                     # YAML file parsing (for configs)

# Jupyter & Notebook Support
jupyter>=1.0.0                  # Jupyter Notebook
jupyterlab>=4.0.0               # JupyterLab
ipywidgets>=8.1.0               # Interactive widgets for notebooks
notebook>=7.0.0                 # Jupyter notebook interface

# File Handling
openpyxl>=3.1.0                 # Excel file reading (for .xls/.xlsx files)
xlrd>=2.0.1                     # Excel file reading (legacy .xls support)

# Type Hints & Development (Optional but recommended)
typing-extensions>=4.8.0        # Extended type hints
mypy>=1.5.0                     # Type checker (optional, for development)

# Performance & Profiling (Optional)
numba>=0.58.0                   # JIT compilation for numerical functions (optional)
psutil>=5.9.0                   # System and process utilities

# Data Validation (Optional)
pydantic>=2.0.0                 # Data validation using Python type annotations

# ============================================================================
# Installation Instructions for Conda Environment
# ============================================================================
#
# 1. Create conda environment with Python 3.9 or 3.10:
#    conda create -n mri_phen python=3.10 -y
#    conda activate mri_phen
#
# 2. Install PyTorch with CUDA support (for RTX 3060):
#    # For CUDA 11.8 (recommended for RTX 3060):
#    conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia -y
#    
#    # OR for CUDA 12.1:
#    conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia -y
#
# 3. Verify CUDA installation:
#    python -c "import torch; print('CUDA available:', torch.cuda.is_available()); print('CUDA version:', torch.version.cuda); print('GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'N/A')"
#
# 4. Install remaining packages:
#    pip install -r requirements.txt
#
# 5. Verify MONAI installation:
#    python -c "import monai; print('MONAI version:', monai.__version__)"
#
# ============================================================================
# Notes
# ============================================================================
# - PyTorch must be installed via conda for proper CUDA support
# - RTX 3060 supports CUDA Compute Capability 8.6
# - Compatible with CUDA 11.0+, but CUDA 11.8 or 12.1 recommended
# - MONAI includes optional dependencies like ITK, gdown, etc.
# - For production, consider pinning exact versions
# ============================================================================

